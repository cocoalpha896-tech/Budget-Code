# Get this from Google Cloud Console → APIs & Services → Credentials
# (see README.md, Step 1) — it is a public identifier, safe to expose client-side.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
